const num1 = 2 // creating a constant variable that accepts a number value
const num2 = 3 //creating a constant variable that accepts a number value

let name = "winifred" //this accepts a string value

const sum = num1 + num2 // creating a constant variable that sums num1 and num2
const subtraction = num2 - num1 // creating a constant variable that subtracts num1 from num2
const multiplication = num1 * num2 // creating a constant variable that multiplies num1 and num2
const division = num2 / num1 // creating a constant variable that divides num2 by num1

console.log("Hello " + name + ", the sum of " + num1 + " and " + num2 + " is " + sum) // prints out the sum of num1 and num2 in a sentence on the console
console.log("Hello " + name + ", the subtraction of " + num2 + " and " + num1 + " is " + subtraction) // prints out the subtraction of num1 from num2 in a sentence on the console
console.log("Hello " + name + ", the multiplication of " + num1 + " and " + num2 + " is " + multiplication) // prints out the multiplication of num1 and num2 in a sentence on the console
console.log("Hello " + name + ", the division of " + num2 + " and " + num1 + " is " + division) // prints out the division of num2 by num1 in a sentence on the console
