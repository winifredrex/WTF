const name = confirm("What is your name?") // this uses booolean value
console.log(name)

let jobtitle = prompt("What is your Job Tile?") // this uses string values and returns what user types in
console.log(jobtitle)